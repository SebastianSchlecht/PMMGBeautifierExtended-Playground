{
  "name": "pmmgtest",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build -c vite_main.config.js && vite build -c vite_serviceWorker.config.js && vite build -c vite_contentScript.config.js",
    "buildSw": "vite build -c vite_serviceWorker.config.js",
    "buildContent": "vite build -c vite_contentScript.config.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "@types/chrome": "^0.0.259"
  }
}
